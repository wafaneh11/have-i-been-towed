# Frontend

Static HTML/CSS/JavaScript served by the FastAPI backend.

- `index.html` — search a plate and view its latest tow record.
- `detection.html` — upload a video and submit it to the CV pipeline.
- `evaluation.html` — review accepted detections and snapshots.

The frontend uses same-origin API calls, so run it through the backend rather than opening the HTML files directly for normal use.
